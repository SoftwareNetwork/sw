// SPDX-License-Identifier: MPL-2.0

//DIR(arh) // archive storage (mirrors etc.)?
//DIR(bin) // files are moved to pkg dir
//DIR(cfg) // moved to etc/sw/checks
//DIR(dat)
DIR(etc)
//DIR(lib) // files are moved to pkg dir
#ifdef _WIN32
//DIR(lnk) // use sw open ...
#endif
//DIR(obj) // files are moved to pkg dir
DIR(pkg)
DIR(tmp)
//DIR(usr)
