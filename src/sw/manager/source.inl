SOURCE(EmptySource, "empty") // sometimes we have everything in the config file
SOURCE(Git, "git")
SOURCE(Mercurial, "hg") // Hg also?
SOURCE(Bazaar, "bzr")
SOURCE(Fossil, "fossil")
SOURCE(Cvs, "cvs")
SOURCE(Svn, "svn")
SOURCE(RemoteFile, "remote")
SOURCE(RemoteFiles, "files")

//darcs,
//perforce aka p4

// do not add local files
